// Online C compiler to run C program online
#include <stdio.h>

// Online C compiler to run C program online
#include <stdio.h>

int main() {
    // Write C code here
      int n,x,b;
    x=1;
    b=0;
    printf("enter the number:");
    scanf("%d",&n);
    while(b<=n)
    {
        b=b;
        x=1;
        while(x<=b)
        {
            printf("%d \t",1);
            x++;
        }
        printf("\n");
        b++;
     }

    return 0;
}